services:
  ai-data-analyst:
    build: .
    container_name: ai-data-analyst
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./sample_data:/app/sample_data
      - ./tmp:/app/tmp
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
